#!/usr/bin/env bash
# This script was deprecated and removed. Use the application endpoint POST /customer/produce/{count}
# to trigger bulk production of messages. Example (produce 10000):
#   curl -X POST http://localhost:8080/customer/produce/10000

echo "This script has been deprecated. Use the application's POST /customer/produce/{count} endpoint to produce messages."

